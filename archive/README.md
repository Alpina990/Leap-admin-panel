# Retired prototype — not executable production code

Historical demo data, spoofable Sites auth, D1 mutations and Cloudflare build tooling are retained only for reference. Nothing here is imported by Next or included by TypeScript. Do not run legacy scripts (especially check-admin.mjs), restore legacy routes, or treat sample data as real. Production commands and rollout are in the root README. No .pen files were accessed.
